# Sports Quiz --- SAC Innovative Project

### Main script is run in Main.py

Notes:
- All other draft .py files are not part of submission, just for fun
- requirements.txt includes dependencies
- sport_data.csv contains survey data
- install dependencies run shell command (do in a virtual environment for best practice):
  $ pip install -r requirements.txt
